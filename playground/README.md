# Playground

Start the API:

```bash
npm run api
```

Open:

- `http://localhost:3000/playground`

Use the form to enter:
- `token`
- `secret`

Then click **Fetch Apartments JSON** to call:

- `GET /apartments`
